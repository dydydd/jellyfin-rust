[package]
name = "jellyfin-media-encoding"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-only"
description = "Media parsing and encoding primitives for Jellyfin"

[lib]
path = "src/lib.rs"

[dependencies]
chardetng = "1.0.0"
chrono.workspace = true
encoding_rs = "0.8"
serde_json.workspace = true

[lints.rust]
unsafe_code = "forbid"
